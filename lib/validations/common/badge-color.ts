/**
 * Type para los colores de badge disponibles
 * Compartido por todos los tipos de status y tags
 */
export type BadgeColor = {
  id: string
  name: string
  key: string
  bgClass: string
  textClass: string
}
